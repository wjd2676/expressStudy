import prismaInstance from "../prisma";

export const createCommentModel = async (boardId: string, comment: string) => {
  return null;
};

export const getCommentListModel = async (boardId: string) => {
  return null;
};

export const updateCommentModel = async (
  boardId: string,
  comment: string,
  isLike: boolean
) => {
  return null;
};

export const deleteCommentModel = async (commentId: string) => {
  return null;
};
